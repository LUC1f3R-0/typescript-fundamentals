const fun = (pre: string, str: Array<string>): Array<string>| string => { 
  
  
  return "";
}


const fun1 = (num: number, numArr: Array<number>): Array<number> | number => {
  
  const newArr = numArr.filter(n => 5 > n);
  return newArr;;
}

console.log(fun1(5, []));